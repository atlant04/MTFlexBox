import XCTest
@testable import MTFlexBox

final class MTFlexBoxTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MTFlexBox().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
